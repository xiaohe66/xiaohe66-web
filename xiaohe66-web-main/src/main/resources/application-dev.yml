debug: true
autologin: true
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/xiaohe66-web?characterEncoding=UTF-8&useUnicode=true&autoReconnect=true&useSSL=false&serverTimezone=GMT
    username: root
    password: root
  thymeleaf:
    cache: false
  resources:
    static-locations: file:${project.build.directory}/res/static/

server:
  port: 8080

logging:
  file:
    path: logs/

xiaohe66:
  wx:
    task:
      miniProgramState: developer
      appId: appid_todo
      appSecret: secret_todo
    love:
      miniProgramState: developer
      appId: appid_love
      appSecret: secret_love